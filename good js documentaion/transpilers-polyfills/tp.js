const transpiller = `
s2s . it reads and understands a source code and translates it to another source code . 
ut can parse modern code and rewrite it in older syntax .
`;

const polyfill = `
adds functions to an old js engine that doesnt have newer js engines functions . 
A script that updates/adds new functions is called “polyfill”.
It “fills in” the gap and adds missing implementations.
`;