const exp = `

    object-oriented : everything is object in javascript except primitive values .
    prototype-based : when we create an array we can use some methods on it . it 
    comes from "prototypal inheritance" . the prototype of an object containes 
    every methods and properties of that .

    EXAMPLE : ARRAY

    Array Prototype :

    -------------------------------
    |   Array                     |
    |                             |
    |   Array.prototype.indexOf   |
    |   Array.prototype.push      |
    |                             |
    -------------------------------
    const arr = [1 ,2 ,3 ,4]

    arr.indexOf(2)
    arr.push(5)

    as see in above when we create an array it inherites everything of its prototype

`;